version: "2"
run:
  concurrency: 4
  modules-download-mode: readonly
  tests: true
linters:
  enable:
    - bodyclose
    - durationcheck
    - gosec
    - noctx
    - unconvert
  settings:
    errcheck:
      check-type-assertions: true
      check-blank: false
      exclude-functions:
        - (*os.File).Close
        - os.Chdir
        - os.Mkdir
        - os.MkdirAll
        - os.Remove
        - os.RemoveAll
        - os.Setenv
        - os.Unsetenv
        - os.Chmod
        - fmt.Fprintf(os.Stderr)
        - fmt.Fprintln(os.Stderr)
        - fmt.Fprintf(os.Stdout)
        - fmt.Fprintln(os.Stdout)
        - (io.Writer).Write
        - fmt.Fprintf
        - fmt.Fprintln
        - (*text/tabwriter.Writer).Flush
    goconst:
      min-occurrences: 5
    gocritic:
      disabled-checks:
        - octalLiteral
        - hugeParam
        - rangeValCopy
        - paramTypeCombine
      enabled-tags:
        - diagnostic
    gosec:
      excludes:
        - G101  # false positive on TOOL_*_DESCRIPTION constant names
        - G104  # unhandled errors — covered by errcheck where it matters
        - G115  # int overflow uintptr→int from os.File.Fd() — safe in practice
        - G204  # subprocess via variable — intentional in ExternalCmdBackend
        - G301  # directory perms — 0755 deliberate for config dir
        - G302  # file perms
        - G304  # file path from variable — reviewed case-by-case
        - G306  # 0644 deliberate for non-secret files
        - G401  # SHA1 — not in our code, pulled by deps
        - G501
        - G703  # path traversal taint — false positive on user-provided config paths
    govet:
      enable:
        - assign
        - atomic
        - bools
        - buildtag
        - cgocall
        - composites
        - copylocks
        - errorsas
        - httpresponse
        - ifaceassert
        - loopclosure
        - lostcancel
        - nilfunc
        - printf
        - shift
        - stdmethods
        - stringintconv
        - structtag
        - tests
        - unmarshal
        - unreachable
        - unusedresult
    misspell:
      locale: US
    nakedret:
      max-func-lines: 30
    whitespace:
      multi-if: false
      multi-func: false
  exclusions:
    generated: lax
    rules:
      - linters:
          - errcheck
          - goconst
          - gocritic
          - gosec
          - ineffassign
          - noctx
          - staticcheck
          - thelper
          - unparam
          - unused
        path: _test\.go
      # QF1xxx are "quickfix" stylistic suggestions from staticcheck v0.26+
      # (embedded-field shortcuts, tagged-switch conversions, etc.). Useful
      # as IDE hints but too noisy to gate CI on.
      - linters:
          - staticcheck
        text: "^QF"
    paths:
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  uniq-by-line: true
formatters:
  exclusions:
    generated: lax
    paths:
      - third_party$
      - builtin$
      - examples$
